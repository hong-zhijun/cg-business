# 修改开发计划：将 account-admin 视为所有者

## 目标

统一项目中的角色判断逻辑，将 `account-admin` (普通管理员) 纳入所有者权限范畴，使其具备不占用名额、不可被踢、显示为所有者等特性。

## 修改步骤

### 1. 数据库层 (`database.py`)

- **操作**: 找到所有排除 `account-owner` 的 SQL 查询。
- **修改逻辑**: 修改查询条件，确保查询结果中同时排除 `account-admin` 和 `primary-owner` 角色，使它们在数据库层面就被视为所有者。

### 2. 后端业务逻辑 (`app_new.py`)

- **操作**: 搜索代码中所有涉及角色判断的逻辑。
- **修改逻辑**:
    1. **成员计数**: 在计算“非所有者成员”数量时，将 `account-admin` 和 `primary-owner` 也加入排除列表，确保它们不占用 Team 名额。
    2. **权限保护**: 在踢人或修改权限的 API 中，增加对 `account-admin` 和 `primary-owner` 的保护，禁止对这些角色执行踢出操作。

### 3. 自动服务 (`auto_kick_service.py`)

- **操作**: 定位到自动清理成员的循环逻辑。
- **修改逻辑**: 增加判断条件，如果成员角色是 `account-admin` 或 `primary-owner`，则直接跳过，不执行踢人逻辑。

### 4. 前端界面 (`templates`)

- **后台成员列表 (`members.html`)**: 修改 JS 中的 `isOwner` 判断逻辑，使其包含 `account-admin`，从而正确显示皇冠图标并隐藏踢出/删除按钮。
- **前台公共页面 (`public_teams.html`)**: 修改模板渲染逻辑，确保 `account-admin` 用户也能显示所有者特有的样式（如紫色背景、皇冠图标）。

## 验证要点

- 确认 `account-admin` 不再占用 4 人名额。
- 确认无法踢出 `account-admin` 成员。
- 确认前端界面上 `account-admin` 显示为所有者样式。
